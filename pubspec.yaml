name: clarity
description: Clarity — Flutter MVVM app with Firebase, offline mode, and notifications
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # --- State management ---
  provider: ^6.1.5+1

  # --- Firebase core packages ---
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.0.3
  firebase_messaging: ^16.0.3
  google_sign_in: ^6.1.5



  # --- Notifications / time ---
  flutter_local_notifications: ^19.4.2
  timezone: ^0.10.1

  # --- Utilities ---
  uuid: ^4.5.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
